rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MODE PENGEMBANGAN: Mengizinkan semua akses bagi pengguna yang login untuk stabilitas total dan kelancaran fitur Admin
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
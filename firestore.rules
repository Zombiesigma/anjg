rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // UNTUK DEVELOPMENT SAJA - izinkan semua akses
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

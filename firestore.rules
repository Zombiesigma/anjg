rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // --- Aturan Pengguna ---
    match /users/{userId} {
      // Siapa pun dapat mengambil profil pengguna tunggal
      allow get: if true;

      // Izinkan pengguna yang diautentikasi untuk melakukan kueri pada koleksi pengguna
      // (diperlukan untuk mencari pengguna berdasarkan nama pengguna)
      allow list: if request.auth != null;

      // Pengguna hanya dapat membuat dan memperbarui profil mereka sendiri
      allow create, update: if request.auth != null && request.auth.uid == userId;

      // Tidak ada yang bisa menghapus profil dari klien
      allow delete: if false;
    }

    // --- Aturan Buku ---
    match /books/{bookId} {
      // Siapa pun dapat mengambil atau membuat daftar buku
      allow get, list: if true;

      // Hanya pengguna dengan peran 'penulis' yang dapat membuat buku
      allow create: if request.auth != null && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'penulis';

      // Hanya penulis buku yang dapat memperbarui atau menghapusnya
      allow update, delete: if request.auth != null && resource.data.authorId == request.auth.uid;

      // --- Aturan Subkoleksi Komentar ---
      match /comments/{commentId} {
        // Siapa pun dapat mengambil atau membuat daftar komentar untuk sebuah buku
        allow get, list: if true;

        // Setiap pengguna yang diautentikasi dapat membuat komentar
        allow create: if request.auth != null;

        // Hanya penulis komentar yang dapat memperbarui atau menghapusnya
        allow update, delete: if request.auth != null && resource.data.userId == request.auth.uid;
      }
    }
  }
}

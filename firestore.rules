// Versi untuk development/testing:
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Izinkan akses publik (HANYA untuk development!)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
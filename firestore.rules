rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MODE PENGEMBANGAN: Mengizinkan semua akses bagi pengguna yang login untuk stabilitas total
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's profile in the system.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID from Firebase Auth."
        },
        "username": {
            "type": "string",
            "description": "The user's unique username."
        },
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the user's profile picture."
        },
        "role": {
            "type": "string",
            "enum": ["pembaca", "penulis", "admin"],
            "description": "The role of the user."
        },
        "bio": {
            "type": "string",
            "description": "A short bio of the user."
        },
        "followers": {
            "type": "number",
            "description": "Number of followers."
        },
        "following": {
            "type": "number",
            "description": "Number of users being followed."
        },
        "status": {
            "type": "string",
            "enum": ["online", "offline"],
            "description": "The user's online status."
        },
        "lastSeen": {
            "type": "string",
            "format": "date-time",
            "description": "The last time the user was seen online."
        },
        "notificationPreferences": {
          "type": "object",
          "description": "User's notification settings.",
          "properties": {
            "onNewFollower": {
              "type": "boolean",
              "description": "Receive notification on new follower."
            },
            "onBookComment": {
              "type": "boolean",
              "description": "Receive notification on book comments."
            },
            "onBookFavorite": {
              "type": "boolean",
              "description": "Receive notification on book favorites."
            },
            "onStoryComment": {
              "type": "boolean",
              "description": "Receive notification on story comments."
            }
          }
        }
      },
      "required": ["uid", "email", "displayName", "username"]
    },
    "Book": {
      "title": "Book",
      "description": "Represents a book in the system.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "synopsis": { "type": "string" },
        "genre": { "type": "string" },
        "coverUrl": { "type": "string", "format": "uri" },
        "viewCount": { "type": "number" },
        "favoriteCount": { "type": "number" },
        "chapterCount": { "type": "number", "description": "Number of chapters in the book." },
        "authorId": { "type": "string" },
        "authorName": { "type": "string" },
        "authorAvatarUrl": { "type": "string", "format": "uri" },
        "status": { "type": "string", "enum": ["draft", "pending_review", "published", "rejected"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "authorId", "authorName", "status", "createdAt"]
    },
    "Chapter": {
        "title": "Chapter",
        "description": "A chapter of a book.",
        "type": "object",
        "properties": {
            "title": { "type": "string" },
            "content": { "type": "string" },
            "order": { "type": "number" },
            "createdAt": { "type": "string", "format": "date-time" }
        },
        "required": ["title", "content", "order", "createdAt"]
    },
    "Comment": {
        "title": "Comment",
        "description": "A comment on a book or a reply to another comment.",
        "type": "object",
        "properties": {
            "text": { "type": "string" },
            "userId": { "type": "string" },
            "userName": { "type": "string" },
            "userAvatarUrl": { "type": "string", "format": "uri" },
            "likeCount": { "type": "number" },
            "replyCount": { "type": "number" },
            "createdAt": { "type": "string", "format": "date-time" }
        },
        "required": ["text", "userId", "userName", "createdAt", "likeCount", "replyCount"]
    },
    "BookCommentLike": {
      "title": "Book Comment Like",
      "description": "Represents a user's like on a book comment.",
      "type": "object",
      "properties": {
          "userId": { "type": "string" },
          "likedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "likedAt"]
    },
    "AuthorRequest": {
      "title": "Author Request",
      "description": "Represents a user's request to become an author.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "portfolio": { "type": "string", "format": "uri" },
        "motivation": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "approved", "rejected"] },
        "requestedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "name", "email", "motivation", "status", "requestedAt"]
    },
    "Chat": {
      "title": "Chat",
      "description": "Represents a chat thread between two or more users.",
      "type": "object",
      "properties": {
        "participantUids": {
          "type": "array",
          "items": { "type": "string" }
        },
        "participants": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "uid": { "type": "string" },
              "displayName": { "type": "string" },
              "photoURL": { "type": "string", "format": "uri" },
              "username": { "type": "string" }
            }
          }
        },
        "lastMessage": {
          "type": "object",
          "properties": {
            "text": { "type": "string" },
            "timestamp": { "type": "string", "format": "date-time" },
            "senderId": { "type": "string" }
          }
        },
        "unreadCounts": {
            "type": "object",
            "description": "Map of userId to their unread message count in this chat.",
            "additionalProperties": { "type": "number" }
        }
      },
      "required": ["participantUids", "participants"]
    },
    "ChatMessage": {
      "title": "Chat Message",
      "description": "Represents a single message within a chat thread. Can be a text message or a special type like a book share.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["text", "book_share"],
          "description": "The type of the message. Defaults to 'text'."
        },
        "text": {
          "type": "string",
          "description": "The content of the message, if it's a 'text' type."
        },
        "senderId": {
          "type": "string"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "book": {
          "type": "object",
          "description": "Information about the shared book, if it's a 'book_share' type.",
          "properties": {
            "id": {
              "type": "string",
              "description": "The ID of the shared book."
            },
            "title": {
              "type": "string",
              "description": "The title of the shared book."
            },
            "coverUrl": {
              "type": "string",
              "format": "uri",
              "description": "The cover URL of the shared book."
            },
            "authorName": {
              "type": "string",
              "description": "The author's name of the shared book."
            }
          }
        }
      },
      "required": [
        "senderId",
        "createdAt",
        "type"
      ]
    },
    "FavoriteBook": {
        "title": "Favorite Book",
        "description": "Marks a book as a favorite for a user. The document ID is the book ID.",
        "type": "object",
        "properties": {
            "userId": { "type": "string" },
            "addedAt": { "type": "string", "format": "date-time" }
        },
        "required": ["userId", "addedAt"]
    },
    "Follow": {
      "title": "Follow",
      "description": "Represents a follow relationship, indicating when a user followed another.",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The UID of the user who initiated the follow." },
        "followedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "followedAt"]
    },
    "Notification": {
      "title": "Notification",
      "description": "Represents a user notification for various events.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["comment", "follow", "favorite", "author_request", "story_comment", "broadcast"]
        },
        "text": { "type": "string" },
        "link": { "type": "string", "description": "The URL to navigate to when the notification is clicked." },
        "actor": {
          "type": "object",
          "properties": {
            "uid": { "type": "string" },
            "displayName": { "type": "string" },
            "photoURL": { "type": "string", "format": "uri" }
          },
          "required": ["uid", "displayName"]
        },
        "read": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["type", "text", "link", "actor", "read", "createdAt"]
    },
    "Story": {
      "title": "Story",
      "description": "A temporary story that disappears after 24 hours.",
      "type": "object",
      "properties": {
        "authorId": { "type": "string" },
        "authorName": { "type": "string" },
        "authorAvatarUrl": { "type": "string", "format": "uri" },
        "content": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "likes": { "type": "number" },
        "commentCount": { "type": "number" },
        "viewCount": { "type": "number", "description": "Number of times the story has been viewed." }
      },
      "required": ["authorId", "authorName", "authorAvatarUrl", "content", "createdAt"]
    },
    "StoryComment": {
      "title": "Story Comment",
      "description": "A comment on a story.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "userAvatarUrl": { "type": "string", "format": "uri" },
        "text": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "userName", "text", "createdAt"]
    },
    "StoryLike": {
      "title": "Story Like",
      "description": "Represents a user's like on a story.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "likedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "likedAt"]
    },
    "StoryView": {
      "title": "Story View",
      "description": "Represents a user's view on a story.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "userAvatarUrl": { "type": "string", "format": "uri" },
        "viewedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "userName", "viewedAt"]
    }
  },
  "auth": {
    "providers": ["password", "google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/UserProfile"
      },
      "description": "Stores user profile information, keyed by their Firebase Auth UID."
    },
    "/users/{userId}/favorites/{bookId}": {
        "schema": { "$ref": "#/backend/entities/FavoriteBook" },
        "description": "Stores a user's favorite books. The document ID is the book ID."
    },
    "/users/{userId}/followers/{followerId}": {
      "schema": { "$ref": "#/backend/entities/Follow" },
      "description": "A list of users that follow this user. The document ID is the follower's user ID."
    },
    "/users/{userId}/following/{followedUserId}": {
      "schema": { "$ref": "#/backend/entities/Follow" },
      "description": "A list of users that this user follows. The document ID is the followed user's ID."
    },
    "/users/{userId}/notifications/{notificationId}": {
      "schema": {
        "$ref": "#/backend/entities/Notification"
      },
      "description": "Stores notifications for a user."
    },
    "/books/{bookId}": {
      "schema": {
        "$ref": "#/backend/entities/Book"
      },
      "description": "Stores book information."
    },
    "/books/{bookId}/chapters/{chapterId}": {
        "schema": {
            "$ref": "#/backend/entities/Chapter"
        },
        "description": "Stores chapters for a specific book."
    },
    "/books/{bookId}/comments/{commentId}": {
      "schema": {
        "$ref": "#/backend/entities/Comment"
      },
      "description": "Stores comments for a specific book."
    },
    "/books/{bookId}/comments/{commentId}/replies/{replyId}": {
      "schema": {
        "$ref": "#/backend/entities/Comment"
      },
      "description": "Stores replies for a specific comment."
    },
    "/books/{bookId}/comments/{commentId}/likes/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/BookCommentLike"
      },
      "description": "Stores likes for a specific comment. Document ID is the user ID."
    },
    "/authorRequests/{requestId}": {
      "schema": {
        "$ref": "#/backend/entities/AuthorRequest"
      },
      "description": "Stores user requests to become an author."
    },
    "/chats/{chatId}": {
      "schema": {
        "$ref": "#/backend/entities/Chat"
      },
      "description": "Stores chat thread metadata."
    },
    "/chats/{chatId}/messages/{messageId}": {
      "schema": {
        "$ref": "#/backend/entities/ChatMessage"
      },
      "description": "Stores messages for a specific chat."
    },
    "/stories/{storyId}": {
      "schema": { "$ref": "#/backend/entities/Story" },
      "description": "Stores user stories, which expire after 24 hours."
    },
    "/stories/{storyId}/comments/{commentId}": {
      "schema": { "$ref": "#/backend/entities/StoryComment" },
      "description": "Stores comments for a specific story."
    },
    "/stories/{storyId}/likes/{userId}": {
      "schema": { "$ref": "#/backend/entities/StoryLike" },
      "description": "Stores likes for a specific story. Document ID is the user ID."
    },
    "/stories/{storyId}/views/{userId}": {
      "schema": { "$ref": "#/backend/entities/StoryView" },
      "description": "Stores views for a specific story. Document ID is the user ID."
    }
  }
}
